# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: [ '7.0.x' ]
        
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ matrix.dotnet-version }}
    - name: Restore dependencies
      run: dotnet restore /src/+Modules/Required/ThePublicLibrary/____Core/TPL.Core/src/TPL.Core.csproj
    - name: Build
      run: dotnet build /src/+Modules/Required/ThePublicLibrary/____Core/TPL.Core/src/TPL.Core.csproj
    - name: Run Unit Tests
      run: dotnet test /src/+Modules/Required/ThePublicLibrary/____Core/TPL.Core.UnitTests/src/TPL.Core.UnitTests.csproj
