namespace WskInfrastructure.IntegrationTests.Data.Books;
public class EfWskGameRepositoryAdd : BaseTestFixture
{
   /*  [Fact]
    public async Task AddsBookSetsId()
    {
        var repository = BookRepository();
        var Book = BookWskTestData.BookAlfradoTheGreat;

        await repository.AddAsync(Book);

        var newBook = (await repository.ListAsync())
                        .FirstOrDefault();

        Assert.NotNull(newBook?.Id);
    } */
}
