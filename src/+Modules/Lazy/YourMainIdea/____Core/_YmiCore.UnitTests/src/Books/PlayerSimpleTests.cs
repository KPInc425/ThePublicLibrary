namespace YmiCore.UnitTests;

public class PlayerSimpleTests : BaseGameTests
{
    /* [Fact]
    public void CanCreateNewGame()
    {
        // I create character name, bio, description, and background. 
        // Create the game using random values based on character data
        // Game has regions, cities, and connected character, max days, time of day.
    }

    [Fact]
    public void CanCreateCharacter()
    {
        // Can create character based on input values, (name, bio, description, background)
        var playerA = new Player(GameTestData.TestPlayer.Name, GameTestData.TestPlayer.Description, GameTestData.TestPlayer.Bio, GameTestData.TestPlayer.UpbringingType);
        playerA.Name.Should().Be(GameTestData.TestPlayer.Name);
        // Have character with these values
    }

    [Fact]
    public void CanCreateTestCharacter()
    {
        // Can create character based on input values, (name, bio, description, background)
        base.ICreateAPlayer(GameTestData.TestPlayer);
        // Have character with these values
        base._playerToTestWith.Name.Should().Be(GameTestData.TestPlayer.Name);
    }

    [Fact]
    public void CanCreateStuff()
    {
        var player = new Player("Shane", "Fancy as f");
        player.Name.Should().Be("Shame", "Because the player name should be Shane");
    } 
    
    */
}