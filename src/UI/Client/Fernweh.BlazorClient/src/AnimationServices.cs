namespace Fernweh.BlazorClient
{
    public static class AnimationServices
    {
        public static void AddServices(WebAssemblyHostBuilder builder)
        {
            /* builder.Services.Configure<AnimateOptions>("LoginButtonAnimation", options =>
               {
                   options.Animation = Animations.FlipDown;
                   options.Delay = TimeSpan.FromSeconds(5);
                   options.Duration = TimeSpan.FromSeconds(1.5);
               });

            builder.Services.Configure<AnimateOptions>(options =>
                {
                    options.Animation = Animations.ZoomIn;
                    options.Duration = TimeSpan.FromMilliseconds(1000);
                }); */
        }
    }
}
